/** Link oficial do grupo de lançamento — usado por todos os CTAs. */
export const WHATSAPP_GROUP_URL =
  "https://chat.whatsapp.com/BfnEyRKFlQYKVRS8Wyq1oG";

/** Largura da prancheta de design. O desktop é renderizado nessa medida
 *  e escalado com `zoom` para caber na viewport (ver DesktopFrame). */
export const DESIGN_WIDTH = 1920;

/** Easing padrão do motion do site. */
export const EASE = [0.22, 1, 0.36, 1] as const;
